diff --git a/auth_service.py b/auth_service.py
--- a/auth_service.py
+++ b/auth_service.py
@@ -86,6 +86,7 @@
     _session_store[token] = {
         "user_id": user_id,
         "email": email,                    # ← PII stored in memory (not the bug here)
         "claims": claims,
         "created_at": time.time(),
+        "last_seen": time.time(),
         # 'last_seen' is intentionally absent here ← root cause of crash
     }
