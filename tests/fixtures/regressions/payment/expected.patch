diff --git a/payment_service.py b/payment_service.py
--- a/payment_service.py
+++ b/payment_service.py
@@ -131,5 +131,2 @@
-    except:                                     # noqa: E722  bare except — BUG
-        # ⚠️  BUG: all exceptions swallowed here; caller sees "succeeded"
-        # ⚠️  P4: no_print_statements
-        print(f"[payment_service] charge failed for order {order_id}, swallowing error")
-        return {"status": "succeeded", "charge_id": None}   # ← silent data corruption
+    except PaymentError:
+        raise
